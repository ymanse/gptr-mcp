# GPT Researcher dependencies
gpt-researcher>=0.14.0
python-dotenv
langchain-google-genai>=4.2.3  # ChatGoogleGenerativeAI for gemini-3.5-flash
ddgs  # DuckDuckGo retriever (keyless fallback when Tavily fails / no API key)

# MCP dependencies
fastmcp>=2.8.0
fastapi>=0.103.1
uvicorn>=0.23.2
pydantic>=2.3.0

# Utility dependencies
loguru>=0.7.0

# Testing dependencies
httpx>=0.24.0
sseclient-py>=1.7.0
